pub fn parse_aml(aml_data: &[u8]) -> Option<()> {
    video::debug!("Parsing {} bytes of AML data.", aml_data.len());
    // TODO: Implement AML parsing logic
    // (just the very basic parts needed)
    None
}
